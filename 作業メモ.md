# ICA Chat 作業メモ

## プロジェクト概要
IBM ICA（IBM Consulting Advantage）APIを使ったチャットUIアプリ。
Quarkus + LangChain4j で構築、macOS ARM向け Native Image 版を GitHub Release に公開。

---

## リポジトリ
- GitHub: https://github.com/TSA0001/55-ICA-con01
- ブランチ: `macarm`
- 公開設定: **Public**

## リリース
- タグ: `v1.0.0-macarm`
- URL: https://github.com/TSA0001/55-ICA-con01/releases/tag/v1.0.0-macarm
- アセット:
  - `ica-chat-macarm-native.zip` ★推奨（38MB、Java不要）
  - `ica-chat-macarm.zip`（23MB、Java 21必要）

---

## 技術スタック
| 項目 | 内容 |
|---|---|
| フレームワーク | Quarkus 3.15.5 |
| AI連携 | LangChain4j 0.26.1（quarkus-langchain4j） |
| API | ICA API（OpenAI互換） `https://api.nextgen-beta.ica.ibm.com/ica/v1/chat-models/` |
| ビルド | GraalVM 21 Native Image |
| 起動時間 | 0.05秒 |
| バイナリサイズ | 98MB |

---

## ファイル構成
```
/Users/tsano/00-works/55-ICA-connect/
├── ica-chat/
│   └── src/main/
│       ├── java/com/ibm/ica/
│       │   ├── ChatResource.java       # REST エンドポイント
│       │   ├── IcaChatService.java     # @RegisterAiService @ApplicationScoped
│       │   └── SessionStore.java       # セッション管理
│       └── resources/
│           ├── application.properties  # 設定ファイル
│           └── META-INF/resources/index.html  # チャットUI
├── dist/
│   ├── ica-chat-macarm-native.zip      # 配布用ZIP（最新）
│   └── ica-chat-macarm-native/
│       ├── setup.command               # 初回セットアップ（quarantine解除）
│       ├── start.command               # 起動スクリプト
│       ├── ica-chat-runner             # Native バイナリ
│       └── 使い方.txt
├── build-mac.sh                        # macOS ビルドスクリプト
├── ica-api-key.txt                     # APIキー（gitignore済み）
└── 作業メモ.md                         # このファイル
```

---

## 主要設定（application.properties）
```properties
quarkus.http.port=8080
quarkus.http.host=localhost            # localhostのみバインド（LAN公開なし）
quarkus.langchain4j.openai.base-url=https://api.nextgen-beta.ica.ibm.com/ica/v1/chat-models/
quarkus.langchain4j.openai.api-key=${ICA_API_KEY}
quarkus.langchain4j.chat-memory.memory-window.max-messages=6
```

---

## 配布手順（ユーザー向け）
```
1. ZIP をダウンロードして展開
   https://github.com/TSA0001/55-ICA-con01/releases/download/v1.0.0-macarm/ica-chat-macarm-native.zip

2. ica-api-key.txt を同フォルダに作成（APIキーを1行記載）

3. 【初回のみ】ターミナルを開いて setup.command をドラッグ＆ドロップ → Enter
   → quarantine 解除 + 実行権限付与

4. start.command をダブルクリック → ブラウザが自動で開く

5. 終了はブラウザの「終了」ボタン
```

---

## 既知の問題・制約
| 項目 | 内容 |
|---|---|
| macOS Quarantine | ダウンロードファイルに quarantine が付く → setup.command で解除が必要 |
| .app 形式 | Gatekeeper に弾かれるため断念（Apple Developer 証明書 $99/年が必要） |
| OS制限 | Native Image は macOS ARM 専用。Windows/Linux は JVM 版を使用 |
| アクセス制限 | localhost のみ（LAN上の他PCからはアクセス不可） |

---

## ビルド方法（Native Image）
```bash
export JAVA_HOME=/Library/Java/JavaVirtualMachines/graalvm-21.jdk/Contents/Home
export PATH=$JAVA_HOME/bin:$PATH
cd ica-chat
./mvnw package -Pnative -DskipTests
# → target/ica-chat-1.0.0-SNAPSHOT-runner が生成される（約90秒）
```

---

## GitHub Release 更新手順
```bash
# 1. バイナリを dist にコピー
cp ica-chat/target/ica-chat-1.0.0-SNAPSHOT-runner dist/ica-chat-macarm-native/ica-chat-runner

# 2. ZIP 作成
cd dist && zip -r ica-chat-macarm-native.zip ica-chat-macarm-native/

# 3. GitHub Release 更新
gh release delete-asset v1.0.0-macarm ica-chat-macarm-native.zip --yes
gh release upload v1.0.0-macarm dist/ica-chat-macarm-native.zip
```

---

## Native Image についての理解メモ
- GraalVM が Java コード → マシン語に変換する
- Quarkus は GraalVM がうまく変換できるよう事前に整理・設定を自動生成する役割
- 実行時は JVM 不要、Quarkus のコードが機械語として焼き込まれて動く
- 「Quarkusベースのアプリ」とは言えるが「JVM上でQuarkusが動いている」わけではない
- 起動が速い理由：通常は起動時に行う初期化処理を、ビルド時に前倒しで済ませているため

---

## 更新履歴
| 日付 | 内容 |
|---|---|
| 2026-06-21 | v1.0.0-macarm リリース |
| 2026-06-21 | localhost バインドに変更（LAN公開防止） |
| 2026-06-21 | setup.command 追加（初回 quarantine 解除） |
| 2026-06-21 | リポジトリを Public に変更 |
